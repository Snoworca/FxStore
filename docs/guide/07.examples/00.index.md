---
name: 예제 목록
description: FxStore 실용 예제 모음
---

# 예제 모음

실제 프로젝트에서 활용 가능한 완전한 코드 예제입니다.

## 예제 목록

| 예제 | 난이도 | 사용 컬렉션 | 설명 |
|------|--------|------------|------|
| [사용자 캐시](01.user-cache.md) | 중급 | Map | TTL 기반 로컬 캐시 구현 |
| [시계열 데이터](02.time-series.md) | 중급 | Map | 센서 데이터 저장 및 집계 |
| [작업 큐](03.task-queue.md) | 고급 | Deque | 영속 작업 큐와 재시도 로직 |
| [세션 스토어](04.session-store.md) | 고급 | Map | 서버 측 세션 관리 |

## 예제별 핵심 기능

### 사용자 캐시
- 커스텀 코덱 구현
- TTL 기반 만료
- Cache-Aside 패턴
- AUTO 모드 활용

### 시계열 데이터
- 타임스탬프 키 설계
- BATCH 모드로 대량 쓰기
- subMap() 범위 조회
- 집계 함수 구현

### 작업 큐
- Deque 양방향 접근
- Dead Letter Queue 패턴
- 재시도 로직
- 워커 스레드 패턴

### 세션 스토어
- 다중 Map 인덱싱
- Lazy 만료 체크
- 세션 속성 관리
- 사용자-세션 매핑

## 공통 패턴

### 코덱 구현 패턴
```java
public class MyCodec implements FxCodec<MyType> {
    @Override
    public byte[] encode(MyType obj) {
        ByteBuffer buffer = ByteBuffer.allocate(size);
        // 고정 길이 필드: putInt, putLong, putDouble
        // 가변 길이: putInt(length) + put(bytes)
        return buffer.array();
    }

    @Override
    public MyType decode(byte[] bytes) {
        ByteBuffer buffer = ByteBuffer.wrap(bytes);
        // encode와 동일한 순서로 읽기
        return new MyType(...);
    }
}
```

### 리소스 관리 패턴
```java
public class MyStore implements AutoCloseable {
    private final FxStore store;

    public MyStore(Path path) {
        this.store = FxStore.open(path, options);
        this.store.registerCodec(MyType.class, new MyCodec());
    }

    @Override
    public void close() {
        store.close();
    }
}

// 사용
try (MyStore store = new MyStore(path)) {
    // 작업 수행
}
```

### 커밋 모드 선택
```java
// 실시간 저장이 중요한 경우
FxOptions.defaults()
    .withCommitMode(CommitMode.AUTO)
    .build();

// 대량 처리가 필요한 경우
FxOptions.defaults()
    .withCommitMode(CommitMode.BATCH)
    .build();
```

## 예제 실행 방법

1. 예제 코드를 프로젝트에 복사
2. FxStore 의존성 추가
3. `main()` 메서드 실행
4. 생성된 `.fx` 파일 확인

## 다음 단계

- [API 레퍼런스](../06.api-reference/fxstore.md) - 전체 API 문서
- [성능 튜닝](../05.tutorials/08.performance.md) - 최적화 방법
- [문제 해결](../08.troubleshooting.md) - 일반적인 문제 해결
